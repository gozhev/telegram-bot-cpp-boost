Telegram-Bot-Cpp-Boost
======================

This is a bot program for the Telegram messeger. It's written in C++17 and uses
the Boost libraries (Boost.Asio, Boost.ProgramOptions, Boost.Beast,
Boost.MySQL). It needs a separate MySQL server as a persistent storage.
